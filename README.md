# Auto Edit Video Skill 🎬

这是一个基于 AI 的自动化视频剪辑与字幕生成工具，专门优化了中文解说类视频的制作流程。

## 🌟 核心功能

- **智能转录**：使用 `fast-whisper` 实现高精度的语音转文字。
- **语义字幕切分**：
  - 优先在标点符号（逗号、句号）处断句。
  - 使用 `jieba` 分词确保不会从词语中间切断。
  - 智能规避“的、了、着”等助词出现在行首。
- **音画自动对齐**：根据音频时间戳自动编排图片序列，支持帧对齐（30FPS）以消除长视频的累计误差。
- **一键合成**：自动调用 FFmpeg 将音频、图片、字幕合成 H.264 编码的高清视频。

## 🛠️ 安装要求

1. **FFmpeg**: 系统需安装 FFmpeg 并配置到环境变量。
2. **Python 依赖**:
   ```bash
   pip install -r requirements.txt
   ```

## 🚀 使用方法

### 1. 生成字幕
```bash
python scripts/transcribe_whisper.py <音频路径> --output_dir <输出目录>
```

### 2. 合成视频
准备好图片素材并命名为 `1.jpg`, `2.jpg`... 放入素材目录，然后运行：
```bash
python scripts/compose_video.py <素材根目录>
```

## 📂 目录结构

```
2nd_video/
├── .gitignore                    # Git 忽略规则
├── .env.example.txt              # 环境变量配置示例
├── requirements.txt              # Python 依赖
├── README.md                     # 项目说明
├── CLAUDE.md                     # Claude Code 项目指南
├── prompt.md                     # Gemini 分镜分析提示词
│
├── .claude/
│   └── skills/
│       └── auto_edit_video/
│           ├── SKILL.md          # Skill 元数据和工作流
│           └── scripts/
│               ├── analyze_gemini.py       # Gemini 分镜分析
│               ├── compose_video.py        # FFmpeg 视频合成
│               ├── debug_sync.py           # 调试工具
│               ├── extract_audio.py        # 音频提取
│               ├── generate_images.py      # 即梦 API 生图
│               ├── jimeng_client.py        # 即梦 API 客户端
│               ├── test_one_image.py       # 单图测试
│               ├── transcribe_funasr.py    # FunASR 转录
│               ├── transcribe_whisper.py   # Whisper 转录
│               └── utils.py                # 工具函数
│
├── raw_materials/                # 原始素材（Git忽略）
│   ├── audios/                   # 提取的音频
│   ├── captions/                 # SRT 字幕
│   ├── copys/                    # 转录文本和分镜 JSON
│   ├── images/                   # AI 生成的图片
│   └── videos/                   # 原始视频输入
│
└── finals/                       # 最终成品输出（Git忽略）
```

## ⚖️ 许可证

MIT License